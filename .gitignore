# Environment variables and secrets
.env
.env.local
.env.*.local

# Docker volumes and persistent data
meshcentral-data/*
!meshcentral-data/config.json.template
!meshcentral-data/userallowedips.txt.example
meshcentral-files/
meshcentral-backup/
meshcentral-web/
mongodb-data/
mongodb-config/
caddy-data/
caddy-config/

# Rendered configuration files (generated from templates)
# Note: meshcentral-data/config.json.template is tracked as the template
# The rendered meshcentral-data/config.json is excluded and generated by render-config.sh

# MongoDB data and logs
mongo-data/
*.log

# Temporary files
*.tmp
*.temp
.DS_Store
Thumbs.db

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# Backup files
*.bak
*.backup

# Docker override files (if you create custom overrides)
docker-compose.override.yml

# SSL/TLS certificates (if manually generated)
*.pem
*.key
*.crt
*.csr
*.pfx
*.p12

# Node modules (if running locally)
node_modules/

# OS files
.Spotlight-V100
.Trashes
ehthumbs.db
Desktop.ini
